import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import FrameworkTabs from '../../../shared/FrameworkTabs.jsx';
import ComponentApiDocs from '../../../shared/ComponentApiDocs.tsx';
import { tableComponentExamples, tableFrameworkExamples } from '../../../dataDisplayDocumentationExamples.ts';
import { tableApiSections } from '../../../dataDisplayDocumentationApiSections.ts';
import * as TableStories from '../../../Table.stories.tsx';

<Meta of={TableStories} />

# Table

행과 열로 구조화된 데이터를 표시합니다. 테두리·줄무늬·밀도·호버 상태와 열 크기, 가로·세로 스크롤, 상단·좌측 고정을 공통 API로 제공합니다.

Vue와 React 구현은 각각 `@uxkm/vue/table`, `@uxkm/react/table`로 제공하며 Nuxt와 Next.js에서도 같은 구현을 재사용합니다.

WebSquare는 `w2:group`과 `w2:textbox`의 `tagname`으로 표 구조를 만들고 반복 행은 DataList와 `w2:generator`에 연결합니다.

## 컴포넌트 구현 코드

아래 코드는 Table의 실제 HTML·Gulp·Vue·React 원본 구현입니다. 열 설정, 스크롤 래퍼, 고정 행·열, 공통 클래스와 전달 속성이 어떻게 결합되는지 확인할 수 있습니다. Next.js는 React, Nuxt는 Vue 구현을 재사용합니다.

<FrameworkTabs examples={tableComponentExamples} showFullCode />

## WebSquare 사용

표·헤더·본문·행·셀 관계는 `tagname="table|thead|tbody|tr|th|td"`와 대응 role로 유지합니다. 행 데이터는 DataList와 generator로 반복하고, 열 너비와 sticky offset은 공통 CSS 변수에 연결합니다. 스크롤·고정 변형은 `table_scroll`과 `table_sticky-*` 클래스를 함께 적용합니다.

## 기본

`columns`로 열 너비·최소 너비·패딩·줄바꿈을 지정하고 내부에는 시맨틱 `thead`·`tbody`를 작성합니다.

<Canvas of={TableStories.Basic} sourceState="none" />
<FrameworkTabs examples={tableFrameworkExamples.basic} />

## 줄무늬

<Canvas of={TableStories.Striped} sourceState="none" />
<FrameworkTabs examples={tableFrameworkExamples.striped} />

## 테두리

<Canvas of={TableStories.Bordered} sourceState="none" />
<FrameworkTabs examples={tableFrameworkExamples.bordered} />

## 컴팩트

셀 패딩을 줄여 많은 데이터를 표시합니다.

<Canvas of={TableStories.Compact} sourceState="none" />
<FrameworkTabs examples={tableFrameworkExamples.compact} />

## 호버

포인터가 위치한 본문 행을 강조합니다.

<Canvas of={TableStories.Hover} sourceState="none" />
<FrameworkTabs examples={tableFrameworkExamples.hover} />

## 스타일 조합

테두리·컴팩트·호버 등의 변형을 함께 사용할 수 있습니다.

<Canvas of={TableStories.Combined} sourceState="none" />
<FrameworkTabs examples={tableFrameworkExamples.combined} />

## 상단 헤더 고정

`scroll`과 `stickyTop`을 조합해 세로 스크롤 중 헤더를 고정합니다.

<Canvas of={TableStories.StickyTop} sourceState="none" />
<FrameworkTabs examples={tableFrameworkExamples.stickyTop} />

## 좌측 열 고정

가로 스크롤 중 첫 번째 열을 고정합니다.

<Canvas of={TableStories.StickyLeft} sourceState="none" />
<FrameworkTabs examples={tableFrameworkExamples.stickyLeft} />

## 다중 열 고정

`stickyCols`와 `stickyLeftOffsets`로 최대 네 개 열을 고정할 수 있습니다.

<Canvas of={TableStories.StickyColumns} sourceState="none" />
<FrameworkTabs examples={tableFrameworkExamples.stickyColumns} />

## 상단·좌측 동시 고정

큰 데이터 표에서 헤더와 식별 열을 동시에 유지합니다.

<Canvas of={TableStories.StickyBoth} sourceState="none" />
<FrameworkTabs examples={tableFrameworkExamples.stickyBoth} />

<ComponentApiDocs sections={tableApiSections} />
