import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import FrameworkTabs from '../../../shared/FrameworkTabs.jsx';
import ComponentApiDocs from '../../../shared/ComponentApiDocs.tsx';
import { statComponentExamples, statFrameworkExamples } from '../../../dataDisplayDocumentationExamples.ts';
import { statApiSections } from '../../../dataDisplayDocumentationApiSections.ts';
import * as StatStories from '../../../Stat.stories.tsx';

<Meta of={StatStories} />

# Stat

핵심 수치와 라벨, 설명, 추세를 강조해 표시합니다. 크기·접두·접미·아이콘·레이아웃·카드 스킨과 반응형 그룹을 제공합니다.

Vue와 React 구현은 각각 `@uxkm/vue/stat`, `@uxkm/react/stat`으로 제공하며 Nuxt와 Next.js에서도 같은 구현을 재사용합니다.

WebSquare는 `w2:group` 안에 label·value·prefix·suffix·description `w2:textbox`를 배치하고 각 값을 화면 데이터에 연결합니다.

## 컴포넌트 구현 코드

아래 코드는 `Stat` 컴포넌트 자체의 구현입니다. React와 Vue는 prop에 따라 구조·크기·카드 스킨·추세 색상을 선택하고, HTML은 같은 구조를 마크업에 직접 적용하며, Gulp는 `stat` Nunjucks macro로 동일한 계약을 제공합니다. Next.js는 React 구현을, Nuxt는 Vue 구현을 그대로 재사용합니다.

<FrameworkTabs examples={statComponentExamples} showFullCode />

## WebSquare 사용

라벨·값·접두·접미·추세 설명은 독립된 `w2:textbox`로 구성해 데이터 바인딩과 갱신 범위를 분리합니다. 여러 지표는 `w2:group.stat_group`으로 묶고, 증감 의미는 텍스트와 `color_*`를 함께 갱신해 색상에만 의존하지 않습니다.

## 기본

<Canvas of={StatStories.Basic} sourceState="none" />
<FrameworkTabs examples={statFrameworkExamples.basic} />

## 접두·접미

통화·백분율·단위 등을 값과 분리해 표현합니다.

<Canvas of={StatStories.PrefixSuffix} sourceState="none" />
<FrameworkTabs examples={statFrameworkExamples.prefixSuffix} />

## 추세

`trend`, `trendColor`, `trendIcon`으로 증감 정보를 표시합니다.

<Canvas of={StatStories.Trend} sourceState="none" />
<FrameworkTabs examples={statFrameworkExamples.trend} />

## 크기

small, medium, large 세 가지 수치 크기를 제공합니다.

<Canvas of={StatStories.Sizes} sourceState="none" />
<FrameworkTabs examples={statFrameworkExamples.sizes} />

## 레이아웃

horizontal, inline, row 클래스로 콘텐츠 관계에 맞는 배치를 선택합니다.

<Canvas of={StatStories.Layouts} sourceState="none" />
<FrameworkTabs examples={statFrameworkExamples.layouts} />

## 아이콘

기본 슬롯·자식 콘텐츠로 아이콘과 커스텀 본문을 조합합니다.

<Canvas of={StatStories.WithIcon} sourceState="none" />
<FrameworkTabs examples={statFrameworkExamples.icon} />

## 카드 스킨

기본, 그림자, 배경형, 좌측 강조 스킨을 조합합니다.

<Canvas of={StatStories.Skins} sourceState="none" />
<FrameworkTabs examples={statFrameworkExamples.skins} />

## 그룹

`stat_group-2`부터 `stat_group-4`, `stat_group-divider`까지 반응형 지표 묶음을 제공합니다.

<Canvas of={StatStories.Group} sourceState="none" />
<FrameworkTabs examples={statFrameworkExamples.group} />

<ComponentApiDocs sections={statApiSections} />
