import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import FrameworkTabs from '../../../shared/FrameworkTabs.jsx';
import ComponentApiDocs from '../../../shared/ComponentApiDocs.tsx';
import { badgeFrameworkExamples } from '../../../dataDisplayFrameworkExamples.ts';
import { badgeApiSections } from '../../../dataDisplayApiSections.ts';
import * as BadgeStories from '../../../Badge.stories.tsx';

<Meta of={BadgeStories} />

# Badge

상태, 분류, 개수를 짧게 표시하는 컴포넌트입니다. 텍스트·상태 점·카운트·다른 요소 위의 겹침 배지를 지원합니다.

Vue와 React 구현은 각각 `@uxkm/vue/badge`, `@uxkm/react/badge`로 제공하며 Nuxt와 Next.js에서도 같은 구현을 재사용합니다.

## 기본

공통 `color_*` 색상으로 배지의 의미를 구분합니다.

<Canvas of={BadgeStories.Basic} sourceState="none" />
<FrameworkTabs examples={badgeFrameworkExamples.basic} />

## 크기

small, medium, large 세 가지 크기를 제공합니다.

<Canvas of={BadgeStories.Sizes} sourceState="none" />
<FrameworkTabs examples={badgeFrameworkExamples.sizes} />

## 상태 점

`dot` 또는 `badge_dot`으로 텍스트 앞에 상태 점을 표시합니다.

<Canvas of={BadgeStories.Dot} sourceState="none" />
<FrameworkTabs examples={badgeFrameworkExamples.dot} />

## 카운트

`count` 또는 `badge_count`로 숫자 폭과 정렬이 안정적인 카운트 배지를 만듭니다.

<Canvas of={BadgeStories.Count} sourceState="none" />
<FrameworkTabs examples={badgeFrameworkExamples.count} />

## 겹침

`BadgeWrap` 안에 기준 요소와 Badge를 배치합니다. 점만 표시할 때도 의미를 알 수 있도록 접근성 이름을 지정합니다.

<Canvas of={BadgeStories.Overlay} sourceState="none" />
<FrameworkTabs examples={badgeFrameworkExamples.overlay} />

<ComponentApiDocs sections={badgeApiSections} />
