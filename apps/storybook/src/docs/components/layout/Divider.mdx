import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import FrameworkTabs from '../../../shared/FrameworkTabs.jsx';
import ComponentApiDocs from '../../../shared/ComponentApiDocs.tsx';
import { dividerFrameworkExamples } from '../../../layoutFrameworkExamples.ts';
import { dividerApiSections } from '../../../layoutApiSections.ts';
import * as DividerStories from '../../../Divider.stories.tsx';

<Meta of={DividerStories} />

# Divider

콘텐츠를 시각적으로 구분하는 구분선입니다. 단순 선·점선·텍스트 레이블·세로 구분을 공통 클래스로 조합합니다.

Vue와 React의 공통 구현은 각각 `@uxkm/vue/divider`, `@uxkm/react/divider`로 제공합니다. Nuxt와 Next.js는 같은 구현을 재사용합니다.

## Playground

점선, 레이블 굵기, 세로 방향, 레이블 정렬과 루트 태그를 Controls에서 조정할 수 있습니다.

<Canvas of={DividerStories.Playground} sourceState="none" />
<FrameworkTabs examples={dividerFrameworkExamples.playground} />

## 기본

`divider` 클래스로 가로 구분선을 그립니다. 레이블이 없는 컴포넌트는 기본적으로 `hr`로 렌더링됩니다.

<Canvas of={DividerStories.Basic} sourceState="none" />
<FrameworkTabs examples={dividerFrameworkExamples.basic} />

## 점선

`dashed` 또는 `divider_dashed`로 점선 스타일을 적용합니다.

<Canvas of={DividerStories.Dashed} sourceState="none" />
<FrameworkTabs examples={dividerFrameworkExamples.dashed} />

## 텍스트 레이블

`label`이나 기본 콘텐츠가 있으면 양쪽 선과 가운데 레이블로 전환됩니다. `plain`은 레이블 굵기를 낮춥니다.

<Canvas of={DividerStories.Text} sourceState="none" />
<FrameworkTabs examples={dividerFrameworkExamples.text} />

## 레이블 정렬

`orient="left"`·`orient="right"`로 레이블 위치를 조정합니다. 기본은 가운데입니다.

<Canvas of={DividerStories.Orient} sourceState="none" />
<FrameworkTabs examples={dividerFrameworkExamples.orient} />

## 세로

`vertical` 또는 `divider_vertical`은 인라인·flex 행 안에서 장식용 세로 구분선으로 사용합니다.

<Canvas of={DividerStories.Vertical} sourceState="none" />
<FrameworkTabs examples={dividerFrameworkExamples.vertical} />

<ComponentApiDocs sections={dividerApiSections} />
