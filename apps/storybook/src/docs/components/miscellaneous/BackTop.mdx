import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import FrameworkTabs from '../../../shared/FrameworkTabs.jsx';
import ComponentApiDocs from '../../../shared/ComponentApiDocs.tsx';
import { backTopFrameworkExamples as examples } from '../../../backTopFrameworkExamples.ts';
import { backTopApiSections } from '../../../backTopApiSections.ts';
import * as Stories from '../../../BackTop.stories.tsx';

<Meta of={Stories} />

# BackTop

스크롤 후 페이지 또는 컨테이너 상단으로 부드럽게 이동하는 플로팅 버튼입니다.

## 기본
<Canvas of={Stories.Basic} sourceState="none" />
<FrameworkTabs examples={examples.basic} />

## 표시 임계값
<Canvas of={Stories.Visibility} sourceState="none" />
<FrameworkTabs examples={examples.visibility} />

## 크기
<Canvas of={Stories.Size} sourceState="none" />
<FrameworkTabs examples={examples.size} />

## 커스텀 콘텐츠
<Canvas of={Stories.Custom} sourceState="none" />
<FrameworkTabs examples={examples.custom} />

## 페이지 스크롤
<Canvas of={Stories.WindowScroll} sourceState="none" />
<FrameworkTabs examples={examples.window} />

## 접근성

아이콘만 사용하는 버튼에는 동작을 설명하는 `aria-label`이 필요합니다. 키보드로 버튼에 접근하고 실행할 수 있어야 합니다.

<ComponentApiDocs sections={backTopApiSections} />
