import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import FrameworkTabs from '../../../shared/FrameworkTabs.jsx';
import ComponentApiDocs from '../../../shared/ComponentApiDocs.tsx';
import { collapseFrameworkExamples as examples } from '../../../collapseFrameworkExamples.ts';
import { collapseApiSections } from '../../../collapseApiSections.ts';
import * as Stories from '../../../Collapse.stories.tsx';

<Meta of={Stories} />

# Collapse

콘텐츠 영역을 접었다 펼 수 있는 컴포넌트입니다. 외부 버튼으로 단일 영역을 제어하거나 패널 그룹으로 여러 섹션을 구성할 수 있습니다.

## 외부 트리거
<Canvas of={Stories.Standalone} sourceState="none" />
<FrameworkTabs examples={examples.standalone} />

## 패널 그룹
<Canvas of={Stories.Basic} sourceState="none" />
<FrameworkTabs examples={examples.basic} />

## 단일 열기
<Canvas of={Stories.AccordionMode} sourceState="none" />
<FrameworkTabs examples={examples.accordion} />

## 스킨
<Canvas of={Stories.Skin} sourceState="none" />
<FrameworkTabs examples={examples.skin} />

## 보조 정보
<Canvas of={Stories.Extra} sourceState="none" />
<FrameworkTabs examples={examples.extra} />

## 비활성
<Canvas of={Stories.Disabled} sourceState="none" />
<FrameworkTabs examples={examples.disabled} />

## 크기
<Canvas of={Stories.Size} sourceState="none" />
<FrameworkTabs examples={examples.size} />

## 슬라이드
<Canvas of={Stories.Slide} sourceState="none" />
<FrameworkTabs examples={examples.slide} />

## 접근성

트리거와 패널은 `aria-expanded`, `aria-controls`, `aria-labelledby`로 연결됩니다. 패널 그룹에서는 `ArrowUp`, `ArrowDown`, `Home`, `End` 키로 활성 트리거 사이를 이동합니다.

<ComponentApiDocs sections={collapseApiSections} />
