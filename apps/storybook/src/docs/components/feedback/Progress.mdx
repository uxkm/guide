import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import FrameworkTabs from '../../../shared/FrameworkTabs.jsx';
import ComponentApiDocs from '../../../shared/ComponentApiDocs.tsx';
import { progressFrameworkExamples as examples } from '../../../progressFrameworkExamples.ts';
import { progressApiSections } from '../../../progressApiSections.ts';
import * as Stories from '../../../Progress.stories.tsx';

<Meta of={Stories} />

# Progress

작업의 진행 상태를 선형 또는 원형으로 표시합니다.

## 기본
<Canvas of={Stories.Basic} sourceState="none" />
<FrameworkTabs examples={examples.basic} />

## 레이블 · 값
<Canvas of={Stories.Label} sourceState="none" />
<FrameworkTabs examples={examples.label} />

## 색상
<Canvas of={Stories.Color} sourceState="none" />
<FrameworkTabs examples={examples.color} />

## 크기
<Canvas of={Stories.Size} sourceState="none" />
<FrameworkTabs examples={examples.size} />

## 줄무늬 · 애니메이션
<Canvas of={Stories.Striped} sourceState="none" />
<FrameworkTabs examples={examples.striped} />

## 불확정
<Canvas of={Stories.Indeterminate} sourceState="none" />
<FrameworkTabs examples={examples.indeterminate} />

## 바 안 텍스트
<Canvas of={Stories.Inside} sourceState="none" />
<FrameworkTabs examples={examples.inside} />

## 원형
<Canvas of={Stories.Circle} sourceState="none" />
<FrameworkTabs examples={examples.circle} />

## 너비
<Canvas of={Stories.Width} sourceState="none" />
<FrameworkTabs examples={examples.width} />

## 접근성

확정 진행률은 `aria-valuenow`, `aria-valuemin`, `aria-valuemax`를 제공하고, 불확정 진행률은 `aria-busy="true"`를 사용합니다.

<ComponentApiDocs sections={progressApiSections} />
