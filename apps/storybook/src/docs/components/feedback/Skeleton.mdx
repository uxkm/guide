import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import FrameworkTabs from '../../../shared/FrameworkTabs.jsx';
import ComponentApiDocs from '../../../shared/ComponentApiDocs.tsx';
import { skeletonFrameworkExamples as examples } from '../../../skeletonFrameworkExamples.ts';
import { skeletonApiSections } from '../../../skeletonApiSections.ts';
import * as Stories from '../../../Skeleton.stories.tsx';

<Meta of={Stories} />

# Skeleton

콘텐츠를 불러오는 동안 실제 레이아웃과 유사한 플레이스홀더를 표시합니다.

## 기본
<Canvas of={Stories.Basic} sourceState="none" />
<FrameworkTabs examples={examples.basic} />

## 형태
<Canvas of={Stories.Shape} sourceState="none" />
<FrameworkTabs examples={examples.shape} />

## 카드
<Canvas of={Stories.CardDemo} sourceState="none" />
<FrameworkTabs examples={examples.card} />

## 정적
<Canvas of={Stories.Static} sourceState="none" />
<FrameworkTabs examples={examples.static} />

## 접근성

로딩 영역은 `role="status"`, `aria-live="polite"`, `aria-busy="true"`를 제공하고 장식용 플레이스홀더는 보조 기술에서 제외합니다. `prefers-reduced-motion` 환경에서는 쉬머 애니메이션이 자동으로 비활성화됩니다.

<ComponentApiDocs sections={skeletonApiSections} />
