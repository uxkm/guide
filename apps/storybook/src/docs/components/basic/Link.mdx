import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import FrameworkTabs from '../../../shared/FrameworkTabs.jsx';
import ComponentApiDocs from '../../../shared/ComponentApiDocs.tsx';
import { linkComponentExamples, linkFrameworkExamples } from '../../../linkFrameworkExamples.ts';
import { linkApiSections } from '../../../linkApiSections.ts';
import * as LinkStories from '../../../Link.stories.tsx';

<Meta of={LinkStories} />

# Link

페이지 이동과 보조 액션에 사용하는 하이퍼링크 컴포넌트입니다. 본문 안 인라인 링크는 Typography의 `typo_link`를 사용합니다.

Vue와 React의 공통 구현은 각각 `@uxkm/vue/link`, `@uxkm/react/link`로 제공합니다. Nuxt와 Next.js는 같은 구현을 재사용합니다.

WebSquare는 URL 이동에 `w2:anchor`, 버튼형 보조 액션에 `xf:trigger`를 사용합니다. Anchor의 표시 텍스트는 `label` 속성이 아니라 자식 `xf:label`에 작성하며, 각 예시의 WebSquare 탭은 화면 루트에서 `w2`, `xf`, `ev` 네임스페이스가 선언되어 있다는 전제의 XML 조각입니다.

## 컴포넌트 구현 코드

아래 코드는 사용 예제가 아니라 `Link` 컴포넌트 자체의 구현입니다. React와 Vue는 전달받은 prop을 공통 CSS 클래스와 접근성 속성으로 변환하고, HTML과 Gulp는 같은 클래스를 마크업에 직접 적용합니다. Next.js는 React 구현을, Nuxt는 Vue 구현을 그대로 재사용합니다.

- `color`·`size`는 허용된 값만 `color_*`·`size_*` 클래스로 변환됩니다.
- `underline`·`noUnderline`·`standalone`·`nav`·`block`·`back`·`iconOnly`는 대응 `link_*` 클래스로 변환됩니다.
- `active`는 `is-active`와 `aria-current="page"`를, `disabled`는 `is-disabled`와 `tabIndex=-1`을 설정합니다.
- `as`로 루트 요소를 바꿀 수 있습니다. `a` 또는 컴포넌트 루트에는 `href`를 전달하고, `button`에는 `type="button"`을 전달합니다.
- React는 `icon`·`iconAfter`, Vue는 `icon`·`icon-after` 슬롯으로 아이콘을 배치합니다.

<FrameworkTabs examples={linkComponentExamples} showFullCode />

## WebSquare 사용

`href`가 있는 링크는 브라우저의 기본 이동 의미를 유지하고, 화면 상태 변경이나 팝업 실행은 `ev:onclick`으로 `scwin` 함수에 연결합니다. 새 창은 `target="_blank"`를 사용하되 opener 제어 등 보안 정책은 WebSquare 프로젝트의 공통 창 열기 정책을 함께 확인합니다. 아이콘은 Icon과 동일하게 프로젝트 정적 리소스 또는 CSS 배경 이미지로 연결합니다.

```javascript
scwin.linkAction_onclick = function () {
  // 팝업 열기 또는 화면 상태 변경 로직을 호출합니다.
};
```

## 기본

기본 색상은 primary이며 hover 시 밑줄이 표시됩니다.

<Canvas of={LinkStories.Basic} sourceState="none" />
<FrameworkTabs examples={linkFrameworkExamples.basic} />

## 이미지 · 아이콘 · 커스텀 요소

children 또는 기본 슬롯에 이미지·아이콘·컴포넌트를 조합할 수 있으며, `as`로 루트 요소를 변경할 수 있습니다.

<Canvas of={LinkStories.CustomContent} sourceState="none" />
<FrameworkTabs examples={linkFrameworkExamples.source} />

## 색상

공통 `color_*` 클래스를 조합합니다. 다른 컴포넌트와 동일한 색상 계약을 사용합니다.

<Canvas of={LinkStories.Colors} sourceState="none" />
<FrameworkTabs examples={linkFrameworkExamples.colors} />

## 크기

공통 `size_*` 클래스로 텍스트 크기를 조절합니다.

<Canvas of={LinkStories.Sizes} sourceState="none" />
<FrameworkTabs examples={linkFrameworkExamples.sizes} />

## 스타일

`underline` · `noUnderline` · `standalone`으로 밑줄과 터치 영역을 조절합니다.

<Canvas of={LinkStories.Styles} sourceState="none" />
<FrameworkTabs examples={linkFrameworkExamples.styles} />

## 내비게이션

`nav` · `active`로 메뉴·탭 링크를 표현합니다. 현재 페이지에는 `aria-current="page"`가 적용됩니다.

<Canvas of={LinkStories.Navigation} sourceState="none" />
<FrameworkTabs examples={linkFrameworkExamples.navigation} />

## 블록 · 그룹

`block`과 `link_group`으로 세로 링크 목록을 구성합니다.

<Canvas of={LinkStories.BlockGroup} sourceState="none" />
<FrameworkTabs examples={linkFrameworkExamples.block} />

## 아이콘 전용

`iconOnly`로 텍스트 없는 링크를 만듭니다. 접근성을 위해 `aria-label`을 반드시 지정합니다.

<Canvas of={LinkStories.IconOnly} sourceState="none" />
<FrameworkTabs examples={linkFrameworkExamples.iconOnly} />

## 아이콘

`icon` · `iconAfter` 또는 Vue의 `#icon` · `#icon-after` 슬롯으로 텍스트 앞뒤에 아이콘을 배치합니다. 장식용 아이콘은 접근성 트리에서 숨깁니다.

<Canvas of={LinkStories.WithIcon} sourceState="none" />
<FrameworkTabs examples={linkFrameworkExamples.icons} />

## 새 창 열기

`target="_blank"`와 `rel="noopener noreferrer"`를 함께 지정합니다. 링크 텍스트에는 새 창 안내를 포함합니다.

<Canvas of={LinkStories.External} sourceState="none" />
<FrameworkTabs examples={linkFrameworkExamples.external} />

## 비활성

`disabled`는 `is-disabled`, `aria-disabled`, `tabindex=-1`을 적용하고 클릭을 차단합니다. HTML에서는 필요에 따라 `href`를 제거하거나 `span`으로 대체합니다.

<Canvas of={LinkStories.Disabled} sourceState="none" />
<FrameworkTabs examples={linkFrameworkExamples.disabled} />

## 문맥 속 사용

알림·카드·폼 등 다른 컴포넌트 안에서 보조 액션 링크로 활용합니다.

<Canvas of={LinkStories.Context} sourceState="none" />
<FrameworkTabs examples={linkFrameworkExamples.context} />

<ComponentApiDocs sections={linkApiSections} />
